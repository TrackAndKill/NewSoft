# Replace YOUR_SUBDOMAIN with the hostname pointed at this VM (e.g. firm.example.com).
# Caddy gets a free Let's Encrypt cert automatically.

YOUR_SUBDOMAIN {
    encode gzip
    handle /api/* {
        reverse_proxy 127.0.0.1:8000
    }
    handle {
        reverse_proxy 127.0.0.1:3000
    }
}
