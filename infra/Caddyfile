# Caddy auto-provisions a Let's Encrypt cert for this hostname.
# Point an A record for firm.profithub.me at this VM's IPv4 first.

firm.profithub.me {
    encode gzip
    handle /api/* {
        reverse_proxy 127.0.0.1:8000
    }
    handle {
        reverse_proxy 127.0.0.1:3000
    }
}
